export interface IUserDetails {
  address: string; // The user's address, e.g., "Surat"
  email: string; // The user's email address, e.g., "Shravanviradiyakb@gmail.com"
  firstName: string; // The user's first name, e.g., "shravn"
  lastName: string; // The user's last name, e.g., "Viradiya"
  profileImageBase64: string; // The user's image encoded in Base64 format
  uid: string; // The user's image encoded in Base64 format
}
